import React, { FC } from 'react';

import s from './ProfileScreen.scss';

export const ProfileScreen: FC = () => {
  return (
    <div>
       <h2>'Профиль пользователя'</h2>
    </div>
  );
};

export default ProfileScreen;
